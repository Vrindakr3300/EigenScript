# GitHub (Linguist) has no EigenScript grammar yet; Python's highlighter is
# the closest visual match (# comments, indentation blocks, strings,
# numbers). Remove once a real grammar is upstreamed to github/linguist.
*.eigs linguist-language=Python
